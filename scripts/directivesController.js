(function() {

    var app = angular.module('app', []);

    var injectParams = ['$scope'];

    var DirectivesController = function ($scope) {
      
    };

    DirectivesController.$inject = injectParams;

    app.controller('DirectivesController', DirectivesController);

}());
